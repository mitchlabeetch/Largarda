openapi: 3.1.0
info:
  title: Largo Email send (stub)
  version: 2026-04-20.0
  x-largo-status: stub
  description: |
    PLACEHOLDER stub for the email tool. Full schema will be
    authored when the corresponding Largo-process client lands.
    Consumers: Wave 5 outreach side-channel.

    Operations to specify:
    #   email.send
components:
  schemas:
    Error:
      type: object
      required: [code, message]
      properties:
        code: { type: string }
        message: { type: string }
        details: { type: object, additionalProperties: true }
paths: {}
