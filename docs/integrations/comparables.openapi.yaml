openapi: 3.1.0
info:
  title: Largo Comparables search (stub)
  version: 2026-04-20.0
  x-largo-status: stub
  description: |
    PLACEHOLDER stub for the comparables tool. Full schema will be
    authored when the corresponding Largo-process client lands.
    Consumers: ma.comparables.search, ma.valuation.draft.

    Operations to specify:
    #   comparables.search
components:
  schemas:
    Error:
      type: object
      required: [code, message]
      properties:
        code: { type: string }
        message: { type: string }
        details: { type: object, additionalProperties: true }
paths: {}
