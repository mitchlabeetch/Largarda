openapi: 3.1.0
info:
  title: Largo Browserless web tool (stub)
  version: 2026-04-20.0
  x-largo-status: stub
  description: |
    PLACEHOLDER stub for the browserless tool. Full schema will be
    authored when the corresponding Largo-process client lands.
    Consumers: ma.briefs.daily, ma.company.qa, ma.valuation.draft.

    Operations to specify:
    #   web.fetch
    #   web.screenshot
    #   web.scrape
components:
  schemas:
    Error:
      type: object
      required: [code, message]
      properties:
        code: { type: string }
        message: { type: string }
        details: { type: object, additionalProperties: true }
paths: {}
