openapi: 3.1.0
info:
  title: Largo Calendar scheduling (stub)
  version: 2026-04-20.0
  x-largo-status: stub
  description: |
    PLACEHOLDER stub for the calendar tool. Full schema will be
    authored when the corresponding Largo-process client lands.
    Consumers: Wave 5 scheduling.

    Operations to specify:
    #   calendar.event.create
    #   calendar.availability.query
components:
  schemas:
    Error:
      type: object
      required: [code, message]
      properties:
        code: { type: string }
        message: { type: string }
        details: { type: object, additionalProperties: true }
paths: {}
