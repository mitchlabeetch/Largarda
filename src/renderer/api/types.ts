// Re-export protocol types for convenient access within the renderer
export type {
  EndpointMap,
  EventMap,
  WsRequest,
  WsResponse,
  WsEvent,
  WsMessage,
  IBridgeResponse,
} from '@aionui/protocol'
